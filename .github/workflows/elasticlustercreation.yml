name: Setup Elasticsearch Cluster on Elastic Cloud
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  setup-elasticsearch:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.8'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Create Elasticsearch deployment
      env:
        ELASTIC_CLOUD_API_KEY: ${{ secrets.ELASTICLOUD_API_KEY }}
      run: |
        python clustercreation.py

    - name: Wait for Elasticsearch to be ready
      env:
        ELASTIC_CLOUD_API_KEY: ${{ secrets.ELASTICLOUD_API_KEY }}
      run: |
        python deploymentstatus.py

    - name: Run Python tests
      env:
        ELASTIC_CLOUD_API_KEY: ${{ secrets.ELASTICLOUD_API_KEY }}
        ELASTIC_CLOUD_USERNAME: ${{ secrets.ELASTICLOUD_USERNAME }}
        ELASTIC_CLOUD_PASSWORD: ${{ secrets.ELASTICLOUD_PASSWORD }}
      run: |
        pytest
